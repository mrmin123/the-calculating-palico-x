# the-calculating-palico-x

### Installation

`npm install` in directory

`webpack-dev-server --content-base build/ --inline` to run on local webserver at `http://localhost:8080/`

`webpack --optimize-minimize` to build minified files
